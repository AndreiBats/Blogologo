import React from "react";
import { StyledButtonReadLater } from "./styles";

interface IProps {
  title: string;
}

export const ButtonReadLater = ({ title }: IProps) => {
  return <StyledButtonReadLater></StyledButtonReadLater>;
};
