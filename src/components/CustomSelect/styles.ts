import { Color } from "ui";

export const CustomStyles = {
  control: () => ({
    display: "flex",
    width: "256px",
    padding: "15px",
    borderRadius: "4px",
    background: Color.ExtraLight,
  }),

  indicatorSeparator: () => ({
    width: 0,
  }),
};
