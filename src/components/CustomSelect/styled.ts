import { Color } from "../../ui";

export const CustomStyles = {
  control: () => ({
    width: "256px",
    display: "flex",
    background: Color.White,
    borderRadius: "4px",
    marginBottom: "64px",
    padding: "15px",
  }),

  indicatorSeparator: () => ({
    width: 0,
  }),
};
