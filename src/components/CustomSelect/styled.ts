import { Color } from "../../ui";

export const CustomStyles = {
  control: () => ({
    width: "256px",
    display: "flex",
    background: Color.ExtraLight,
    borderRadius: "4px",
    padding: "15px",
  }),

  indicatorSeparator: () => ({
    width: 0,
  }),
};
