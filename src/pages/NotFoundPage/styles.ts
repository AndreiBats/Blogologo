import styled from "styled-components";

const StyledNotFoundPage = styled.div`
  text-align: center;
`;

const Title = styled.h2`
  margin-bottom: 36px;
  font-size: 46px;
`;

const Description = styled.h3`
  font-size: 36px;
`;

export { StyledNotFoundPage, Title, Description };
